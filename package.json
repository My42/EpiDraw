{
  "scripts": {
    "api.lint": "lerna exec npm run lint --scope api",
    "api.test": "docker-compose -f docker-compose.test.yml up --abort-on-container-exit",
    "lint": "concurrently 'npm:api.lint'",
    "start": "docker-compose -f docker-compose.yml up",
    "test": "concurrently 'npm:api.test'"
  },
  "name": "root",
  "private": true,
  "devDependencies": {
    "concurrently": "^5.0.0",
    "lerna": "^3.19.0"
  }
}
