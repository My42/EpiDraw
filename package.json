{
  "scripts": {
    "preinstall": "lerna bootstrap",
    "api": "lerna exec yarn start --scope api",
    "api-test": "lerna exec yarn test --scope api",
    "client": "lerna exec yarn start --scope client",
    "start": "concurrently 'npm:api' 'npm:client'",
    "test": "concurrently 'npm:api-test'"
  },
  "name": "root",
  "private": true,
  "devDependencies": {
    "concurrently": "^5.0.0",
    "lerna": "^3.16.4"
  }
}
