{
  "scripts": {
    "auth.lint": "lerna exec npm run lint --scope auth",
    "auth.test": "lerna exec npm test",
    "auth.install": "lerna exec npm install",
    "auth.update": "lerna exec npm update",
    "lint": "concurrently 'npm:auth.lint'",
    "start": "docker-compose -f docker-compose.yml up",
    "test": "concurrently 'npm:auth.test'"
  },
  "name": "root",
  "private": true,
  "devDependencies": {
    "concurrently": "^5.0.0",
    "lerna": "^3.19.0"
  }
}
