{
  "scripts": {
    "gateway.lint": "lerna exec npm run lint --scope gateway",
    "gateway.test": "docker-compose -f docker-compose.gateway.test.yml up --abort-on-container-exit",
    "lint": "concurrently 'npm:gateway.lint'",
    "start": "docker-compose -f docker-compose.yml up",
    "test": "concurrently 'npm:gateway.test'"
  },
  "name": "root",
  "private": true,
  "devDependencies": {
    "concurrently": "^5.0.0",
    "lerna": "^3.19.0"
  }
}
