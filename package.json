{
  "scripts": {
    "auth.lint": "lerna exec npm run lint --scope auth",
    "auth.test": "docker-compose -f docker-compose.auth.test.yml up --abort-on-container-exit",
    "lint": "concurrently 'npm:auth.lint'",
    "start": "docker-compose -f docker-compose.yml up",
    "test": "concurrently 'npm:auth.test'"
  },
  "name": "root",
  "private": true,
  "devDependencies": {
    "concurrently": "^5.0.0",
    "lerna": "^3.19.0"
  }
}
